<!DOCTYPE html>
<html lang="da">

<head>
    <meta charset="UTF-8">
    <title>Portefølje Transaktioner</title>
    <link rel="stylesheet" href="/portfoliotransactions_style.css">
</head>

<body>
    <h1>Transactions in your portfolios</h1>

    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Portfolio</th>
                <th>Stock</th>
                <th>Purchase price</th>
                <th>Sale price</th>
                <th>Quantity purchased</th>
                <th>Quantity sold</th>
                <th>Type</th>
            </tr>
        </thead>
        <tbody>
          <% if (transactions.length === 0) { %>
            <tr>
              <td colspan="8">Ingen transaktioner fundet.</td>
            </tr>
          <% } else { %>
            <% for (var i = 0; i < transactions.length; i++) { 
                 var t = transactions[i]; %>
              <tr>
                <td><%= t.getFormattedDate() %></td>
                <td><%= t.portfolio %></td>
                <td><%= t.stock %></td>
                <td><%= t.buyPrice ? t.buyPrice.toFixed(2) + " DKK" : "-" %></td>
                <td><%= t.sellPrice ? t.sellPrice.toFixed(2) + " DKK" : "-" %></td>
                <td><%= t.bought %></td>
                <td><%= t.sold %></td>
                <td><%= t.getType() %></td>
              </tr>
            <% } %>
          <% } %>
        </tbody>
    </table>

    <a href="/portfolios">
        <button type="button">Back to portfolios</button>
    </a>
</body>

</html>